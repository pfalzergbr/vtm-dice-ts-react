import { useDice } from '../useDice';


describe('generateRandomDice', () => {
  // const { generateRandomDice } = useDice()


  test('should return a number between 0 and 9', () => {
    // const result = generateRandomDice();
    // expect(result).to
  })
  
  test('should return an integer', () => {
    
  })
  

})



describe('When rolling with only normal dice', () => {
  test('should ', () => {
    
   
  })
  


})



describe('When rolling with normal and hunger dice', () => {
  
})
